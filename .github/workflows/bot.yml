name: Run Telegram Bot with Neural Network

on:
  schedule:
    - cron: '*/10 * * * *'  # запуск каждые 10 минут
  workflow_dispatch:        # возможность ручного запуска вручную

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
      with:
        persist-credentials: false  # 🔥 отключает github-actions[bot]

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -r bot/requirements.txt
        pip install transformers torch sentencepiece  # Добавляем зависимости для нейросети

    - name: Cache Hugging Face models
      uses: actions/cache@v3
      with:
        path: ~/.cache/huggingface
        key: ${{ runner.os }}-huggingface-${{ hashFiles('bot/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-huggingface-

    - name: Run bot
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      run: |
        python bot/bot.py

    - name: Commit updated files
      env:
        GH_PAT: ${{ secrets.GH_PAT }}
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"
        git remote set-url origin https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}
        git add public/news.html public/sitemap.xml public/rss.xml seen_ids.txt
        git commit -m "Update news.html, sitemap.xml, rss.xml and seen_ids from bot" || echo "No changes"
        git push